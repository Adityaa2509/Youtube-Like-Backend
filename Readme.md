# Pro Backend Project 

1. app.use() with middlewares
2. cors to allow our backed to communicate with frontend
3. cookie-parser --> 
4. app.use(express.json({
    limit:"16kb"
}))(form se data aaye)
5. Now a days body parser are not needed express is now smart enough (form se data aaye )
6. Multer is a third party package to upload files on backend

7. Ab url se data aaye to thoda complex hota hai

aditya kuntal ==> (aditya+kuntal) or (aditya%20kuntal)
app.use(urlencoded({
    extended:true ==> object ke andar object
}))

8. static ==> fevicon/images and all
9. this.data me kya hots hai broo

10. Will use bcrypt ==> a library which helps to hash the password,

11. jsonwebtoken ==> creates token

12. jwt is a bearer token ==> means jo usko bear krta hai use sahi maan lete hai humm
    jsike pass hai wohi uska malik hai

13. Rfersh token me dta kmm hota hai,access token and refresh token dono jwt token hi hai    